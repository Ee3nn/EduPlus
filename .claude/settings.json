{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/ethan/Desktop/Compass7 && git add -A && git diff --cached --quiet && exit 0; git commit -m 'Auto-commit changes' && git push origin HEAD",
            "timeout": 30,
            "statusMessage": "Auto-committing and pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
